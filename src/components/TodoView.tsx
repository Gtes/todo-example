import React from "react";
import TodosContainer from "../lib/API/components/TodosContainer";

const TodoView: React.FC = () => {
    return (
        <>
            <TodosContainer />
        </>
    );
};

export default TodoView;
