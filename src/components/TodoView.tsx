import React from "react";
import TodosContainer from "../lib/components/TodosContainer";

const TodoView: React.FC = () => {
    return (
        <>
            <TodosContainer />
        </>
    );
};

export default TodoView;
